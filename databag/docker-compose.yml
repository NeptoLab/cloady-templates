services:
  databag:
    image: balzack/databag:latest
    ports:
      - 7000
    volumes:
      - databag-databag:/var/lib/databag
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  databag-databag:
    driver_opts:
      size: 2Gi
