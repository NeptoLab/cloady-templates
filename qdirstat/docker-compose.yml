services:
  qdirstat:
    image: lscr.io/linuxserver/qdirstat:1.8.1-ls82
    ports:
      - 3000
    volumes:
      - host:/host:ro
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  host:
    driver_opts:
      size: 2Gi
