services:
  ddns-updater:
    image: qmcgaw/ddns-updater:v2.10.0
    ports:
      - 8000
    volumes:
      - config:/updater/data
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  config:
    driver_opts:
      size: 2Gi
