services:
  mind:
    image: mrcas/mind:v1.4.1
    environment:
      TZ: ${TZ}
    ports:
      - 8080
    volumes:
      - mind-db:/app/db
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  mind-db:
    driver_opts:
      size: 2Gi
