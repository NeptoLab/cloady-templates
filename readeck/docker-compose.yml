services:
  readeck:
    image: codeberg.org/readeck/readeck:latest
    ports:
      - 8000
    volumes:
      - readeck-readeck:/readeck
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  readeck-readeck:
    driver_opts:
      size: 2Gi
