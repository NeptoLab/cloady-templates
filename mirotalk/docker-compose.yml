services:
  mirotalk:
    image: mirotalk/p2p:latest
    ports:
      - 3000
    volumes:
      - env:/src/.env:ro
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  env:
    driver_opts:
      size: 2Gi
