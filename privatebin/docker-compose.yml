services:
  privatebin:
    image: privatebin/nginx-fpm-alpine:2.0.4
    ports:
      - 8080
    volumes:
      - data:/srv/data
    deploy:
      resources:
        reservations:
          cpus: 0.25
          memory: 256M
volumes:
  data:
    driver_opts:
      size: 2Gi
